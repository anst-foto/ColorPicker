<Window x:Class="ColorPicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ColorPicker"
        
        Title="MainWindow" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Vertical"
                    DockPanel.Dock="Top">
            <local:InputComponent x:Name="Red"
                                  LabelText="Red"
                                  Action="Red_OnAction"/>
            <local:InputComponent x:Name="Green"
                                  LabelText="Green"
                                  Action="Green_OnAction"/>
            <local:InputComponent x:Name="Blue"
                                  LabelText="Blue"
                                  Action="Blue_OnAction"/>
            <local:InputComponent x:Name="Alpha"
                                  LabelText="Alpha"
                                  Action="Alpha_OnAction"></local:InputComponent>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" 
                    DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal">
                <Label Content="RGBA"
                       Target="RGBA"/>
                <TextBox x:Name="RGBA"
                         IsReadOnly="True"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="HEX"
                       Target="HEX"/>
                <TextBox x:Name="HEX"
                         IsReadOnly="True"/>
            </StackPanel>
        </StackPanel>
        
        <Rectangle x:Name="Rectangle"/>
    </DockPanel>
</Window>
